const proxy = require('http-proxy-middleware')
 
module.exports = function(app) {
  //app.use(proxy('/api', { target: 'http://localhsot:8020' }))
  //app.use(proxy('/batches', { target: 'http://localhsot:8008/' }))
}