import React from 'react';
// import Header from '../components/Header/header';

const Layout = (props) => {
	return <div style={{ direction: 'ltr' }}>{props.children}</div>;
};

export default Layout;
