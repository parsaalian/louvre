import React from 'react';
// import Header from '../components/Header/header';

const Layout = (props) => {
	return <div style={{ direction: 'rtl' }}>{props.children}</div>;
};

export default Layout;
