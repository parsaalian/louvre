syntax = "proto3";

message Painting {
    string owner = 1;
    repeated float gene = 2;
    bool forSale = 4;
    // Timestamp created_at = 5;
}

message PaintingContainer {
    repeated Painting entries = 1;
}
