syntax = "proto3";
import "louvre/player.proto";

message Painting {
    required Player owner = 1;
    required repeated float gene = 2;
    float offered_price = 3;
    bool for_sale = 4;
    // Timestamp created_at = 5;
}

message PaintingContainer {
    repeated Painting entries = 1;
}
