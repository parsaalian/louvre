syntax = "proto3";
import "louvre/painting.proto";

message Player {
    required string publickey = 1;
    required string username = 2;
    required float balance = 3 [default = 0];
    // repeated Painting gallery = 4;
}

message PlayerContainer {
    repeated Player entries = 1;
}
